import { TrendingUp } from 'lucide-react'
import { Bar, BarChart, CartesianGrid, Cell, XAxis, YAxis } from 'recharts'

import {
	Card,
	ChartConfig,
	ChartContainer,
	ChartTooltip,
	ChartTooltipContent,
	CardContent,
	CardDescription,
	CardFooter,
	CardHeader,
	CardTitle
} from '@/shared/ui'


export const revalidate = 60

export default async function BarChartStats() {
	
}
